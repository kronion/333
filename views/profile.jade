doctype html
html(lang="en")
  head
    include base_head
  body
    include banner
    div.content
      each link in links
        p= link
      each source in image_sources
        img(src = source, height='42', width='42')
    include page_scripts
